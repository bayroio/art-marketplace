import { AccessTokenProvider, BitskiEngine, Network } from 'bitski-provider';
import { ProviderOptions } from '../bitski';
export declare class BitskiBrowserEngine extends BitskiEngine {
    private network;
    private webBaseUrl;
    private apiBaseUrl;
    private tokenProvider;
    private clientId;
    private sdkVersion;
    private headers;
    private rpcHeaders;
    private signer;
    constructor(clientId: string, tokenProvider: AccessTokenProvider, sdkVersion: string, network: Network, options?: ProviderOptions);
    protected addSubproviders(): void;
}
