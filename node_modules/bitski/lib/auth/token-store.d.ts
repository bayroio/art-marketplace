import { TokenResponse } from '@openid/appauth';
import { Store } from '../utils/store';
import { AccessToken } from './access-token';
export declare class TokenStore {
    readonly currentToken: string | undefined;
    readonly refreshToken: string | undefined;
    protected readonly accessTokenKey: string;
    protected readonly refreshTokenKey: string;
    protected store: Store;
    protected accessToken?: AccessToken;
    protected clientId: string;
    constructor(clientId: string, store?: Store);
    persistTokenResponse(response: TokenResponse): void;
    invalidateCurrentToken(): void;
    clear(): void;
}
