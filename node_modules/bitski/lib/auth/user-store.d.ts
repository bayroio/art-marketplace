import { Store } from '../utils/store';
import { User } from './user';
export declare class UserStore {
    readonly currentUser: User | undefined;
    protected readonly storageKey: string;
    protected store: Store;
    protected clientId: string;
    protected user?: User;
    constructor(clientId: string, store?: Store);
    set(user: User | undefined): void;
    clear(): void;
    protected fetchUser(): User | undefined;
    protected cacheUser(user: User | undefined): void;
}
