import { Subprovider } from '@bitski/provider-engine';
import { AuthProvider } from '../auth/auth-provider';
export declare class AuthenticatedCacheSubprovider extends Subprovider {
    private authProvider;
    private cachedValues;
    constructor(authProvider: AuthProvider);
    handleRequest(payload: any, next: any, end: any): any;
    private supportsCache;
    private checkCachedValues;
    private getAccounts;
}
