import { GeneralAssetLedgerAbility, SuperAssetLedgerAbility } from '@0xcert/scaffold';
import { AssetLedger } from '../core/ledger';
export default function (ledger: AssetLedger, accountId: string): Promise<any[] | (SuperAssetLedgerAbility | GeneralAssetLedgerAbility | -1)[]>;
