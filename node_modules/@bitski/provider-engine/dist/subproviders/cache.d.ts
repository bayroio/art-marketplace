import Web3ProviderEngine from '..';
import { JSONRPCRequest } from '../base-provider';
import Subprovider, { CompletionHandler, NextHandler } from '../subprovider';
export default class BlockCacheProvider extends Subprovider {
    private isEnabled;
    private strategies;
    constructor();
    setEngine(engine: Web3ProviderEngine): void;
    handleRequest(payload: JSONRPCRequest, next: NextHandler, end: CompletionHandler): void;
    _handleRequest(payload: JSONRPCRequest, next: (cb?: any) => void, end: (error: Error | null, result?: any) => void): void;
    protected clearOldCache(newBlock: any): void;
}
