import { JSONRPCRequest } from '../base-provider';
import { CompletionHandler, NextHandler } from '../subprovider';
import FilterSubprovider, { FilterSubproviderOptions } from './filters';
export default class SubscriptionSubprovider extends FilterSubprovider {
    protected subscriptions: any;
    constructor(opts?: FilterSubproviderOptions);
    handleRequest(payload: JSONRPCRequest, next: NextHandler, end: CompletionHandler): void;
    protected eth_subscribe(payload: JSONRPCRequest, cb: (error: Error | null, result?: any) => void): void;
    protected eth_unsubscribe(payload: JSONRPCRequest, cb: (error: Error | null, result?: any) => void): void;
    private _notificationHandler;
    private _notificationResultFromBlock;
}
